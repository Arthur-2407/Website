FROM nginx:alpine

# Install curl for healthcheck
RUN apk add --no-cache curl

# Copy nginx config into the image (avoids Docker Desktop Windows bind-mount path issues)
COPY nginx.conf /etc/nginx/nginx.conf

# Expose ports
EXPOSE 80 443
